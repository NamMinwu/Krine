spring:
  datasource:
    url: jdbc:h2:mem:krine-test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
llm:
  provider: mock
  gemini:
    api-key: ""
    model: gemini-2.0-flash
  openai:
    api-key: ""
    model: gpt-4o
